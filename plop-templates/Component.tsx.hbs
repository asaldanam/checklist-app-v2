import React from 'react';
import styled from 'styled-components';

interface Props {
  children?: any;
}

const UI{{pascalCase name}}: React.FC<Props> = (props: Props) => {
  return (
    <React.Fragment>
      {{pascalCase name}}
    </React.Fragment>
  );
};

export default UI{{pascalCase name}};